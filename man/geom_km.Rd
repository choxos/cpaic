% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{geom_km}
\alias{geom_km}
\title{Kaplan-Meier curves from the survival data behind a cML-NMR fit}
\usage{
geom_km(object, ..., curve_args = list(), cens_args = list())
}
\arguments{
\item{object}{A \code{\link[=cmlnmr]{cmlnmr()}} fit with \code{family = "survival"}.}

\item{...}{Passed to \code{\link[ggplot2:geom_step]{ggplot2::geom_step()}}.}

\item{curve_args, cens_args}{Optional lists of arguments customizing the
curves and the censoring marks.}
}
\value{
A list of ggplot2 layers.
}
\description{
Ported from \code{multinma::geom_km()} (Phillippo et al. 2020). Returns a list of
ggplot2 layers, so it can be added to an existing plot; adding it to
\code{\link[=plot_survival]{plot_survival()}} overlays the observed data on the fitted survival curves.
}
\details{
Only status \code{1} counts as an event; statuses \code{0}, \code{2}, and \code{3} (right, left,
and interval censoring) are treated as censored for the empirical curve.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot_survival(fit) + geom_km(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=plot_survival]{plot_survival()}}
}
