% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend.R
\name{posterior_summary}
\alias{posterior_summary}
\title{Posterior summary of a component ML-NMR fit}
\usage{
posterior_summary(x, variables = NULL, ...)
}
\arguments{
\item{x}{A \code{cpaic_mlnmr} fit from \code{\link[=cmlnmr]{cmlnmr()}}.}

\item{variables}{Character vector of Stan variable names to summarize, for
example \code{"tau"} or \code{c("beta", "gamma")}. Naming a block returns one row per
element of it. The default summarizes every sampled block the fit has.}

\item{...}{Further summary functions passed to
\code{posterior::summarise_draws()}, for example \code{"quantile2"} or a function.
With none given, the default set is returned.}
}
\value{
A data frame with one row per scalar parameter. With the default
summaries the columns are \code{variable}, \code{mean}, \code{median}, \code{sd}, \code{mad}, \code{q5},
\code{q95}, \code{rhat}, \code{ess_bulk}, and \code{ess_tail}. Because both backends are
summarized through the same code, \code{rhat}, \code{ess_bulk}, and \code{ess_tail} are
the same quantities whichever engine produced the fit.
}
\description{
Summarizes the posterior draws of a \code{\link[=cmlnmr]{cmlnmr()}} fit: the component effects
\code{beta}, the component by effect-modifier interactions \code{gamma}, the study
baselines \code{mu}, the heterogeneity \code{tau}, and any other sampled block, with
the usual convergence quantities alongside.

Use this rather than reaching into \code{fit$fit}. That object is whatever the
sampler backend returned: an S4 \code{stanfit} under \code{backend = "rstan"} and an R6
object under \code{backend = "cmdstanr"}. The two share no accessors, so
\code{fit$fit$summary(...)} works on one and fails on the other. This function
works on both and returns the same columns either way.
}
\examples{
\dontrun{
fit <- cmlnmr(ipd, agd, effect_modifiers = "x1", inactive = "Placebo")
posterior_summary(fit, "tau")
min(posterior_summary(fit)$ess_bulk)
}
}
\seealso{
\code{\link[=cmlnmr]{cmlnmr()}} for the fit, \code{\link[=relative_effects]{relative_effects()}} and
\code{\link[=component_effects]{component_effects()}} for effects on the outcome scale rather than the
parameters themselves, and \code{\link[=redact_fit]{redact_fit()}}, which strips the draws.
}
