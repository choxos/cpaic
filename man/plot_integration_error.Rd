% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{plot_integration_error}
\alias{plot_integration_error}
\title{Numerical integration error against the number of integration points}
\usage{
plot_integration_error(
  x,
  ...,
  int_thin = NULL,
  ndraws = 200L,
  show_expected_rate = TRUE
)
}
\arguments{
\item{x}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{...}{Unused.}

\item{int_thin}{Report the error every \code{int_thin} points. Default is
\code{n_int / 8}, rounded up.}

\item{ndraws}{Number of posterior draws to summarize over. Default \code{200}.}

\item{show_expected_rate}{Draw the \code{1/N} convergence envelope? Default
\code{TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
Ported from \code{multinma::plot_integration_error()} (Phillippo et al. 2020).
Aggregate arms are fitted by integrating the individual-level model over the
study's covariate distribution with Sobol' quasi-Monte-Carlo points. The
integration error at \code{N} points is the estimate using the first \code{N} points
minus the estimate using all of them; the typical convergence rate of QMC
integration, \code{1/N}, is drawn for reference. If the error has not settled well
inside the \code{1/N} envelope by \code{n_int}, refit with more integration points.
}
\details{
cpaic does not save cumulative integration points inside Stan, so the
integrated aggregate-arm quantity is reconstructed here from the posterior
draws and the (deterministic) Sobol' point set. This is exact, not an
approximation, but it is not free: subsample the posterior with \code{ndraws} on a
large fit.

Not available for \code{family = "survival"}, where the aggregate contribution is
a \code{log_sum_exp} over integration points of the likelihood rather than an
integrated mean outcome; multinma declines this plot for survival models too.
Nor for a Gaussian model fitted with all-normal margins, which is \emph{exact} at
the covariate means and uses a single integration point.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot_integration_error(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=cmlnmr]{cmlnmr()}}
}
