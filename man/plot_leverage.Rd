% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{plot_leverage}
\alias{plot_leverage}
\title{Leverage plot}
\usage{
plot_leverage(object, ..., dic_contours = 1:4)
}
\arguments{
\item{object}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{...}{Unused.}

\item{dic_contours}{Numeric vector of DIC contours to draw. Default \code{1:4}.}
}
\value{
A \code{ggplot} object.
}
\description{
Ported from \code{multinma::plot.nma_dic()} with \code{type = "leverage"} (Phillippo et
al. 2020). Each data point's leverage (its contribution to the effective
number of parameters) is plotted against its signed square root residual
deviance, with contours of constant DIC contribution. Points outside the
\code{DIC = 3} contour are usually the ones spoiling the fit.
}
\details{
Leverage is the pointwise \code{pV} penalty, half the posterior variance of the
point's deviance, matching the penalty used by \code{\link[=dic]{dic()}}. Because deviances
covary across points, the pointwise leverages need not sum exactly to the
model's total \code{pV}.

Leverage plots need a saturated model and so are \strong{not available for survival
outcomes}, where the censored contributions have no saturated reference.
multinma declines them for the same reason.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot_leverage(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dic]{dic()}}, \code{\link[=plot.cpaic_dic]{plot.cpaic_dic()}}
}
