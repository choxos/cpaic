% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{plot.cpaic_dic}
\alias{plot.cpaic_dic}
\title{Deviance and dev-dev plots}
\usage{
\method{plot}{cpaic_dic}(x, y = NULL, ..., labels = c("Model 1", "Model 2"))
}
\arguments{
\item{x}{A \code{cpaic_dic} object from \code{\link[=dic]{dic()}}.}

\item{y}{An optional second \code{cpaic_dic} object, for a dev-dev plot.}

\item{...}{Unused.}

\item{labels}{Model names used on the axes of a dev-dev plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Ported from \code{multinma::plot.nma_dic()} (Phillippo et al. 2020). With a single
\code{\link[=dic]{dic()}} object the plot shows each data point's contribution to the posterior
mean deviance; points contributing much more than the rest are fitted poorly.
With two \code{\link[=dic]{dic()}} objects it draws the \strong{dev-dev plot}: points below the line
of equality fit better under the second model, points above it fit better
under the first.
}
\details{
\code{\link[=dic]{dic()}} stores the posterior mean deviance per data point. The deviance and
the residual deviance differ by a per-point constant that is common to both
models, so the line of equality keeps its meaning. For posterior uncertainty
and for the leverage plot, which need the saturated model, call
\code{\link[=plot_leverage]{plot_leverage()}} on the fitted model itself.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot(dic(fit_fe), dic(fit_re), labels = c("Fixed", "Random"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=dic]{dic()}}, \code{\link[=plot_leverage]{plot_leverage()}}
}
