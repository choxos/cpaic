% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{bridge_fragility}
\alias{bridge_fragility}
\title{Bridge fragility: how much cross-sub-network drift would change a conclusion}
\usage{
bridge_fragility(
  object,
  treatment,
  comparator = NULL,
  newdata = NULL,
  threshold = 0,
  plausible_drift = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{treatment, comparator}{The contrast to assess. \code{comparator} defaults to
the fit reference.}

\item{newdata}{A one-row data frame giving the target population's
effect-modifier values (required when the model has effect modifiers).}

\item{threshold}{Decision boundary on the link scale. Default \code{0} (no effect).}

\item{plausible_drift}{Optional per-component drift bound (link scale) at
which to report the posterior probability that the conclusion is robust.}

\item{...}{Unused.}
}
\value{
An object of class \code{cpaic_fragility}: the contrast, the L1 drift
loading, the posterior of the bridge fragility threshold, and (if
\code{plausible_drift} is given) the probability the conclusion survives it.
}
\description{
In a disconnected network the cross-gap contrast exists only because the
component effects are assumed constant \emph{across} sub-networks. That assumption
cannot be tested from the data, because there is no cross-gap evidence.
\code{bridge_fragility()} quantifies how sensitive a requested contrast is to a
violation of it.
}
\details{
On the linear-predictor scale the contrast is \eqn{D = m'(\beta + \Gamma x)}
with \eqn{m = C_t - C_u}. A cross-sub-network drift \eqn{\Delta} in the
component effects shifts it to \eqn{D + m'\Delta}. Bounding each component's
drift by \eqn{|\Delta_c| \le d}, the worst-case shift is \eqn{d \sum_c |m_c|},
so the smallest per-component drift that moves the contrast to a decision
threshold \eqn{\tau} (default 0, on the link scale) is the \strong{bridge fragility
threshold}
\deqn{\mathrm{BFT} = |D - \tau| / \textstyle\sum_c |m_c|,}
reported per posterior draw. A small BFT means a clinically trivial amount of
un-testable drift would overturn the conclusion. This is a conservative
worst-case over the component \emph{main-effect} drift; interaction drift
\eqn{\Lambda} is not included, so the true fragility is no larger than
reported.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
bridge_fragility(fit, treatment = "A+B", newdata = data.frame(x1 = 0))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=relative_effects]{relative_effects()}}, \code{\link[=estimable_effects_at]{estimable_effects_at()}}
}
