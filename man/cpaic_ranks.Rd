% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranks.R
\name{cpaic_ranks}
\alias{cpaic_ranks}
\title{Treatment and component hierarchies at target covariate means}
\usage{
cpaic_ranks(
  object,
  newdata = NULL,
  what = c("treatment", "component"),
  set = NULL,
  lower_is_better = FALSE,
  include_screen_only = FALSE,
  estimand = "average_conditional_link",
  ...
)
}
\arguments{
\item{object}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{newdata}{A one-row data frame giving target effect-modifier means.
Required when the model has effect modifiers.}

\item{what}{\code{"treatment"} (default) or \code{"component"}. Ranking components by
their incremental effect is only meaningful in an additive model.}

\item{set}{Optional character vector restricting the elements to rank (the
set \code{S} of Wigle et al.). Defaults to all treatments (or all components).}

\item{lower_is_better}{If \code{TRUE}, a smaller effect is preferred (e.g.
mortality). Default \code{FALSE} (a larger effect is preferred).}

\item{include_screen_only}{If \code{FALSE} (default), elements whose relative
effect is identified only by aggregate arms (a first-order screen that can
be optimistic under a nonlinear link) are excluded from the hierarchy and
reported in the \code{dropped_screen} attribute. Set \code{TRUE} to rank them as an
explicitly exploratory hierarchy.

The test here is whether the individual patient data identify the element,
which is not identical to \code{basis == "exact"} in \code{\link[=estimable_effects_at]{estimable_effects_at()}}.
That column additionally excludes \strong{survival} from \code{"exact"}, because a
flexible baseline hazard adds support-dependent nuisance parameters the
covariate-support argument does not see. Survival elements identified by IPD
are therefore still ranked by default; dropping every survival element from
every survival hierarchy would leave nothing to rank. Read a survival
hierarchy alongside \code{\link[=estimable_effects_at]{estimable_effects_at()}} rather than on its own.}

\item{estimand}{The only implemented value is
\code{"average_conditional_link"}. Marginal standardized rankings are not yet
implemented and are rejected explicitly.}

\item{...}{Unused.}
}
\value{
A data frame, ordered from most to least preferred, with columns
\code{element}, \code{estimate} (posterior mean of the relative effect versus the
reference, on the link scale), \code{p_best}, \code{median_rank}, \code{mean_rank} and
\code{sucra}. The \code{dropped} attribute lists elements excluded as not estimable
at these target means.
}
\description{
Ranks treatments or components using average conditional link-scale effects
evaluated at supplied effect-modifier means. Because the component contrast
is linear in those means, the hierarchy can change with them. This is not a
hierarchy of marginal effects standardized over a covariate distribution.
}
\details{
Elements whose relative effect is not estimable at those target means are
\strong{dropped from the ranking set} rather than ranked from a prior-driven
posterior, and are reported in the \code{dropped} attribute. This is Step 3 of the
Wigle et al. workflow, and it matters more here than in the aggregate-data
case, because the estimable set depends on the target means (see
\code{\link[=estimable_effects_at]{estimable_effects_at()}}).

Ranking metrics depend on the set being ranked, so they are not comparable
across different sets. Report them alongside the relative effects, never
instead of them.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
# Which component ranks best when the target mean of x1 is 0.5?
cpaic_ranks(fit, newdata = data.frame(x1 = 0.5), what = "component")
\dontshow{\}) # examplesIf}
}
\references{
Wigle A, Beliveau A, Nikolakopoulou A, Lin L (2026). Creating Treatment and
Component Hierarchies in Component Network Meta-Analysis.
}
\seealso{
\code{\link[=estimable_effects_at]{estimable_effects_at()}}, \code{\link[=relative_effects]{relative_effects()}}
}
