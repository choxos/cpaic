% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{edge_influence}
\alias{edge_influence}
\title{Does the individual patient data actually inform this contrast?}
\usage{
edge_influence(object, treatment, comparator = NULL, tol = 1e-08, ...)
}
\arguments{
\item{object}{A \code{cpaic_bridge}, \code{cpaic_maic} or \code{cpaic_stc} object.}

\item{treatment, comparator}{The contrast of interest. \code{comparator} defaults
to the network reference.}

\item{tol}{Influence weights below this (relative to the largest) are treated
as zero.}

\item{...}{Unused.}
}
\value{
A data frame with one row per edge: \code{studlab}, \code{treat1}, \code{treat2},
\code{has_ipd}, and \code{influence} (the weight \code{w_j}). Edges are ordered by
absolute influence. A warning is issued if any IPD edge has no influence on
the requested contrast.
}
\description{
Population adjustment only helps if the adjusted edges actually \emph{carry} the
contrast you care about. They need not. In a component bridge the estimate of
a contrast \verb{m' beta} is a weighted combination of the observed edges,
}
\details{
\deqn{m'\hat\beta = \underbrace{m' (X'WX)^{+} X'W}_{w} \, d ,}

so edge \code{j} influences the answer only through its weight \code{w_j}. An IPD edge
with \code{w_j} of zero contributes nothing to that contrast, and adjusting it
changes nothing.

The weight uses a diagonal \code{W} of inverse edge variances. The fit itself is
produced by \code{netmeta::discomb()}, which accounts for the within-study
covariance of a multi-arm trial, so in a network containing multi-arm studies
the weight reported here is a close approximation to the fitted estimator's
influence rather than its exact value. It is intended as a screening
diagnostic, to flag an IPD edge that carries little or no weight on the
contrast; read a weight near zero as "this edge barely matters here", not as
an exact sensitivity.

This matters because the usual diagnostic cannot detect the problem. In
simulation, putting the IPD on an edge that does not bridge the gap left
cMAIC numerically identical to the unadjusted analysis (bias +0.374, coverage
0.676) while \code{\link[=effective_sample_size]{effective_sample_size()}} happily reported an ESS of 999 out of
1000. A healthy ESS says the \emph{weights} are well behaved; it says nothing about
whether the reweighted edge is relevant to your estimand.
}
\examples{
net <- cpaic_network(cpaic_bin_agd, sm = "OR", inactive = "Placebo")
br <- cnma_bridge(net)
edge_influence(br, treatment = "A+B+C")
}
\seealso{
\code{\link[=effective_sample_size]{effective_sample_size()}}, \code{\link[=estimable_effects]{estimable_effects()}}
}
