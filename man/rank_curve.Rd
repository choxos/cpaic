% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranks.R
\name{rank_curve}
\alias{rank_curve}
\title{How a hierarchy changes across target populations}
\usage{
rank_curve(
  object,
  em,
  values,
  at = NULL,
  what = c("treatment", "component"),
  lower_is_better = FALSE,
  include_screen_only = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{em}{Name of the effect modifier to vary.}

\item{values}{Numeric vector of target values for \code{em}.}

\item{at}{Optional named vector fixing the other effect modifiers. Defaults
to 0 for each.}

\item{what, lower_is_better, include_screen_only}{See \code{\link[=cpaic_ranks]{cpaic_ranks()}}.}

\item{...}{Unused.}
}
\value{
A data frame with one row per (element, target value), giving \code{sucra},
\code{mean_rank}, \code{p_best} and \code{estimate}, plus \code{estimable}.
}
\description{
Recomputes \code{\link[=cpaic_ranks]{cpaic_ranks()}} over a grid of target populations, so that the
population dependence of the hierarchy is visible. Under population adjustment
a component's rank is a function of the target, and this is the object that
shows it.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
rank_curve(fit, em = "x1", values = seq(-1, 1, by = 0.25), what = "component")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=cpaic_ranks]{cpaic_ranks()}}
}
