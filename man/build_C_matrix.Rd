% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_setup.R
\name{build_C_matrix}
\alias{build_C_matrix}
\title{Build a component-coded treatment-by-component matrix}
\usage{
build_C_matrix(treatments, sep.comps = "+", inactive = NULL)
}
\arguments{
\item{treatments}{Character vector of (unique) treatment labels.}

\item{sep.comps}{Single character separating components in a treatment
label. Default \code{"+"}.}

\item{inactive}{Optional name of the inactive/reference treatment or
component (mapped to a zero row / dropped as a column).}
}
\value{
A binary matrix with one row per treatment and one column per
component (treatments as row names, components as column names).
}
\description{
Splits multi-component treatment labels (e.g. \code{"A + B"}) on \code{sep.comps}
and returns the binary treatment-by-component design matrix \code{C}, where
\code{C[t, c] = 1} if treatment \code{t} contains component \code{c}. The \code{inactive}
component (e.g. placebo) is represented by an all-zero row, matching the
convention of \code{\link[netmeta:netcomb]{netmeta::netcomb()}}.
}
\examples{
build_C_matrix(c("A", "B", "A + B", "placebo"), inactive = "placebo")
}
