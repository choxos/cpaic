% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connectivity.R
\name{estimable_effects}
\alias{estimable_effects}
\title{Which relative effects of a component network are uniquely estimable?}
\usage{
estimable_effects(object, reference = NULL, ...)
}
\arguments{
\item{object}{A \code{\link[=cpaic_network]{cpaic_network()}}, \code{\link[=cpaic_connectivity]{cpaic_connectivity()}}, \code{cpaic_bridge}
or \code{cpaic_mlnmr} object.}

\item{reference}{Reference treatment. Defaults to the network reference.}

\item{...}{Unused.}
}
\value{
A data frame with one row per treatment, giving the \code{treatment},
the \code{comparator} (the reference), and \code{estimable} (logical).
}
\description{
The additive component model identifies a relative effect
\verb{theta_i - theta_j = (C_i - C_j)' beta} only when the contrast vector
\code{C_i - C_j} lies in the \strong{row space} of the design matrix \verb{X = B C}
(Wigle et al. 2026). Full column rank of \code{X} (rank equal to the number of
components) is \emph{sufficient} for every contrast to be estimable, but it is
\strong{not necessary}: a disconnected, rank-deficient component network can
still identify many cross-sub-network treatment contrasts.
}
\details{
Checking this matters because both engines otherwise return a
finite-looking answer for a contrast that carries no information: the
frequentist weighted least squares through the Moore-Penrose pseudoinverse,
and the Bayesian model through the prior.
}
\examples{
net <- cpaic_network(cpaic_bin_agd, sm = "OR", inactive = "Placebo")
estimable_effects(net)
}
\references{
Wigle A, Beliveau A, Nikolakopoulou A, Lin L (2026). Creating Treatment and
Component Hierarchies in Component Network Meta-Analysis.
}
