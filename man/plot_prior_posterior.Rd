% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{plot_prior_posterior}
\alias{plot_prior_posterior}
\title{Prior versus posterior}
\usage{
plot_prior_posterior(x, ..., prior = NULL, bins = 40)
}
\arguments{
\item{x}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{...}{Unused.}

\item{prior}{Which priors to show. Any of \code{"intercept"} (\code{mu}), \code{"beta"}
(component effects), \code{"regression"} (\code{breg}), \code{"gamma"} (component by
effect-modifier interactions), and \code{"tau"} (heterogeneity, random effects
only). Defaults to all that the model used.}

\item{bins}{Number of histogram bins for the posterior. Default \code{40}.}
}
\value{
A \code{ggplot} object.
}
\description{
Ported from \code{multinma::plot_prior_posterior()} (Phillippo et al. 2020).
Posteriors are drawn as histograms, priors as lines. Where a posterior simply
reproduces its prior, the data carry no information about that parameter, and
any quantity that leans on it is prior-driven rather than estimated. This is
the visual counterpart of \code{\link[=prior_sensitivity]{prior_sensitivity()}}.
}
\details{
It matters most for the component by effect-modifier interactions \code{gamma}:
interactions informed only by aggregate arms are weakly identified, and
\code{prior_gamma_scale} then does real regularization.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot_prior_posterior(fit, prior = "gamma")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=prior_sensitivity]{prior_sensitivity()}}, \code{\link[=prior_predictive_check]{prior_predictive_check()}}
}
