% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_edge_influence}
\alias{plot_edge_influence}
\title{Plot how much each edge informs a chosen contrast}
\usage{
plot_edge_influence(object, treatment, comparator = NULL, ...)
}
\arguments{
\item{object}{A \code{cpaic_bridge}, \code{cpaic_maic}, or \code{cpaic_stc} object.}

\item{treatment, comparator}{The contrast of interest. \code{comparator} defaults
to the network reference.}

\item{...}{Passed to \code{\link[=edge_influence]{edge_influence()}} (for example \code{tol}).}
}
\value{
A \code{ggplot} object.
}
\description{
Visualizes \code{\link[=edge_influence]{edge_influence()}}: the weight with which each observed edge
enters the estimate of one relative effect. This plot exists because the
usual population-adjustment diagnostics cannot see the failure it detects.
An IPD edge with \strong{zero influence} on your contrast contributes nothing to
it, so reweighting that edge cannot move the answer, however healthy its
effective sample size looks. Such edges are drawn in red and labelled.
}
\details{
There is no counterpart to this plot in multinma; it is specific to the
component bridge, where a contrast is a weighted combination of edges chosen
by the component design rather than by the network path.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
net <- cpaic_network(cpaic_bin_agd, ipd = cpaic_bin_ipd, sm = "OR",
                     family = "binomial", ipd_covariates = "x1",
                     inactive = "Placebo")
br <- cnma_bridge(net)
plot_edge_influence(br, treatment = "A+B+C")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=edge_influence]{edge_influence()}}, \code{\link[=effective_sample_size]{effective_sample_size()}}
}
