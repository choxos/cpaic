% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-mlnmr.R
\name{plot.cpaic_mlnmr}
\alias{plot.cpaic_mlnmr}
\title{MCMC diagnostics for a cML-NMR fit}
\usage{
\method{plot}{cpaic_mlnmr}(
  x,
  y,
  ...,
  type = c("trace", "density", "hist", "pairs", "rhat", "neff"),
  pars = NULL
)
}
\arguments{
\item{x}{A \code{\link[=cmlnmr]{cmlnmr()}} fit.}

\item{y}{Unused, for compatibility with the \code{\link[=plot]{plot()}} generic.}

\item{...}{Passed to the underlying \code{bayesplot} function.}

\item{type}{\code{"trace"} (default), \code{"density"}, \code{"hist"}, \code{"pairs"}, \code{"rhat"},
or \code{"neff"}.}

\item{pars}{Character vector of parameter names to show. Defaults to the
component effects \code{beta}, the interactions \code{gamma}, and, for a
random-effects model, the heterogeneity \code{tau}.}
}
\value{
A \code{ggplot} object (\code{"pairs"} returns a \code{bayesplot} grid).
}
\description{
Ported from \code{multinma::plot.stan_nma()} (Phillippo et al. 2020), which hands
the posterior draws to \code{bayesplot}. Diverging transitions, a high maximum
\code{Rhat}, or a low effective sample size all mean the posterior has not been
explored, and nothing downstream of it is trustworthy.
}
\examples{
\dontshow{if (FALSE) withAutoprint(\{ # examplesIf}
plot(fit, type = "trace")
plot(fit, type = "pairs", pars = c("beta[1]", "tau[1]"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=cmlnmr]{cmlnmr()}}, \code{\link[=plot_prior_posterior]{plot_prior_posterior()}}
}
