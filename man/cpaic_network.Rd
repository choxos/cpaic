% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_setup.R
\name{cpaic_network}
\alias{cpaic_network}
\title{Set up a (possibly disconnected) component network for cpaic}
\usage{
cpaic_network(
  agd,
  ipd = NULL,
  treat1 = "treat1",
  treat2 = "treat2",
  TE = "TE",
  seTE = "seTE",
  studlab = "studlab",
  sm,
  inactive = NULL,
  sep.comps = "+",
  reference = NULL,
  family = NULL,
  ipd_study = ".study",
  ipd_trt = ".trt",
  ipd_outcome = ".y",
  ipd_time = NULL,
  ipd_status = NULL,
  ipd_exposure = NULL,
  ipd_covariates = NULL
)
}
\arguments{
\item{agd}{Aggregate (contrast-level) data frame.}

\item{ipd}{Optional individual patient data frame (one row per patient).
Must contain a study column, a treatment column, an outcome, and the
effect-modifier / prognostic covariates.}

\item{treat1, treat2, TE, seTE, studlab}{Column names in \code{agd}.}

\item{sm}{Summary measure (e.g. \code{"OR"}, \code{"RR"}, \code{"MD"}, \code{"HR"}), passed
to \code{\link[netmeta:discomb]{netmeta::discomb()}}.}

\item{inactive}{Name of the inactive component / reference (e.g.
\code{"placebo"}).}

\item{sep.comps}{Component separator in treatment labels. Default \code{"+"}.}

\item{reference}{Reference treatment for reported relative effects.
Defaults to \code{inactive} when available, otherwise the first treatment.}

\item{family}{Outcome family for the IPD model, one of \code{"binomial"},
\code{"gaussian"}, \code{"poisson"}, \code{"survival"} (required when \code{ipd} is given).}

\item{ipd_study, ipd_trt, ipd_outcome}{Column names in \code{ipd}. For survival
outcomes use \code{ipd_time}/\code{ipd_status} instead of (or alongside)
\code{ipd_outcome}; for Poisson rates an optional \code{ipd_exposure} offset.}

\item{ipd_time, ipd_status}{Time and event-indicator column names in
\code{ipd} (survival family).}

\item{ipd_exposure}{Optional exposure/person-time column in \code{ipd}
(Poisson family); used as a log offset.}

\item{ipd_covariates}{Character vector of covariate column names in
\code{ipd} (the candidate effect modifiers / prognostic factors).}
}
\value{
An object of class \code{cpaic_network}: a list with elements
\code{agd}, \code{ipd}, \code{treatments}, \code{components}, \code{C.matrix}, \code{sm},
\code{inactive}, \code{reference}, \code{sep.comps}, \code{family}, and the column-name
mappings.
}
\description{
Builds the network object consumed by \code{\link[=cnma_bridge]{cnma_bridge()}}, \code{\link[=cmaic]{cmaic()}},
\code{\link[=cstc]{cstc()}} and (Phase 2) \code{cmlnmr()}. Aggregate data are supplied at the
\strong{contrast level} (one row per pairwise comparison: \code{treat1} vs
\code{treat2}, with a treatment effect \code{TE} and standard error \code{seTE}),
matching the input convention of \code{\link[netmeta:discomb]{netmeta::discomb()}}. Individual
patient data (IPD) are optional and used only by the population-adjusted
methods to replace a study's unadjusted contrast with an adjusted one.
}
\details{
Treatment labels encode components via \code{sep.comps} (e.g. \code{"A + B"}), so
that sub-networks sharing components can be bridged.
}
\examples{
# Aggregate-only, disconnected network
net <- cpaic_network(cpaic_bin_agd, sm = "OR", inactive = "Placebo")
net

# With individual patient data for the population-adjusted methods
net_ipd <- cpaic_network(cpaic_bin_agd, ipd = cpaic_bin_ipd, sm = "OR",
                         family = "binomial", ipd_covariates = "x1",
                         inactive = "Placebo")
}
\seealso{
\code{\link[=cpaic_connectivity]{cpaic_connectivity()}}, \code{\link[=cnma_bridge]{cnma_bridge()}}, \code{\link[=cmaic]{cmaic()}}, \code{\link[=cstc]{cstc()}}
}
